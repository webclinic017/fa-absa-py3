[Securities Lending ABSA]FMultiInstrumentAndTrades:slMarginDirection = select(groupingReference.IsKindOf("FParty"), 
  false->nil, 
  default->select(groupingReference.AdditionalInfo.SL_CptyType == "Borrower", 
    true->select((slMarginCall * slCollateralHaircutCash) > 0, 
      true->"Due to YOU", 
      false->"Due to US"), 
    false->select(groupingReference.AdditionalInfo.SL_CptyType == "Lender", 
      true->select((slMarginCall * slCollateralHaircutCash) < 0, 
        true->"Due to YOU", 
        false->"Due to US"), 
      default->nil), 
    default->nil));