[Securities Lending ABSA]FTrade:sobRegulatoryNotionalCurrency = select((collapse(sobRegulatoryNotional) == nil) or (collapse(sobRegulatoryNotional).Unit == nil), 
  true->nil, 
  default->currencyFromUnit(collapse(sobRegulatoryNotional).Unit));