#grouping: enabled

[Core Risk]FObject:correlation =
  Description=Shift of all correlation values in a scenario. The specified shift value(s) are interpreted as absolute numbers of percentage points added to the current correlation values. Note that all types of correlations, except quanto (FX) correlations, are shifted.
  ExtensionAttributeGroupItem=correlation
  FilterType=FCorrelationMatrix
  GroupLabel=Correlation
  MatchTopNodeOnly=false
  ShiftFunctions=shiftCorrelationMatrix

