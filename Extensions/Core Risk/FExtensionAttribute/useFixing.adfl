[Core Risk]FCashFlowInstrument:useFixing = switch(isFinite(marketPrice), 
  true->true, 
  false->false);
[Core Risk]FRateIndex:useFixing = switch(isFinite(floatRateReferencePrice), 
  true->switch(ignoreFixingInRateIndex, 
    false->true, 
    true->false), 
  false->false);