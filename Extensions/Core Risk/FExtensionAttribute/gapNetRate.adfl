[Core Risk]FTimeBucketAndObject:gapNetRate = switch(double(gapNet) <> 0, 
  true->sumOf(stripDateTimePart(gapNetRateBucket)) / gapNet, 
  false->switch(gapNet == nil, 
    true->nil, 
    false->0.0));