[GenericMandates]FBusinessProcess:mandatesQueryFolders = 
# Retrieve the query folders linked to a Mandate
py("GenericMandatesColumns", context).GetMandateQueryFoldersFromBP(object);
[GenericMandates]FLimit:mandatesQueryFolders = 
# Retrieve the query folders linked to a Mandate
py("GenericMandatesColumns", context).GetMandateQueryFolders(object);