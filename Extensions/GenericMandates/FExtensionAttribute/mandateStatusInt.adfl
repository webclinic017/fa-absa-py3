[GenericMandates]FBusinessProcess:mandateStatusInt = switch(object.CurrentStep.State.Name, 
  "Active"->1, 
  "Rejected"->2, 
  "Error"->3, 
  default->4);
[GenericMandates]FLimit:mandateStatusInt = switch(limitCurrentState, 
  "Active"->1, 
  "Ready"->2, 
  default->2);