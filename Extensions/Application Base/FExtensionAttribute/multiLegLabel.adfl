[Application Base]FLegAndTrades:multiLegLabel = select(instrument:payOrRecLabel, 
  true->select(leg.PayLeg, 
    true->"Pay " + currency, 
    false->"Rec " + currency), 
  false->select(leg.IsFirstStartDayLeg, 
    true->"First " + currency, 
    false->"Second " + currency));
[Application Base]FTradeRow:multiLegLabel = "Total " + displayCurrency;