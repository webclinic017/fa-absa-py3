[_upload_base_exts]FBusinessProcess:lastComment = shunt(notes.Size > 0, 
  true->notes.Last, 
  default->nil);
[_upload_base_exts]FBusinessProcessMultiItem:lastComment = shunt(notes.Size > 0, 
  true->notes.Last, 
  default->nil);