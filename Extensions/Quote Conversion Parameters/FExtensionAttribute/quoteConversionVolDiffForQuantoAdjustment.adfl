[Quote Conversion Parameters]FInstrument:quoteConversionVolDiffForQuantoAdjustment = select(isQuanto and not isForwardStart, 
  true->volatilityForQuantoAdjustedCarryCost - volatilityValueModel, 
  default->0.0);