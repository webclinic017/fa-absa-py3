[Portfolio Trading]FInstrumentAndTrades:parentValueNormalized = select(needsConversion, 
  true->convertToCurrency(parentValueDenominated, object:theoreticalValueMtmFxRates [fxRateInstrument = fxRateParent], displayCurrencySymbol, filterUnit), 
  default->parentValue);