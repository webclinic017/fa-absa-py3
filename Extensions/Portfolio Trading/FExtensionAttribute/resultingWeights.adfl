[Portfolio Trading]FInstrumentAndTrades:resultingWeights = switch(parent:childrenHasInput [settings, targetColumnSetExternally, distributionTypeSetExternally], 
  false->unchangedWeight, 
  default->switch(actionInverseVariable, 
    nil->unchangedWeight * weightAdjustment, 
    default->actionInverseVariable / parentValue));