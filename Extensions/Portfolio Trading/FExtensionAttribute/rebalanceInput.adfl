[Portfolio Trading]FMultiInstrumentAndTrades:rebalanceInput = ref(parent:rebalanceInput);
[Portfolio Trading]FPortfolioInstrumentAndTrades:rebalanceInput = select(parent, 
  default->ref(parent:rebalanceInput), 
  nil->var(nil));
[Portfolio Trading]FSingleInstrumentAndTrades:rebalanceInput = ref(parent:rebalanceInput);
[Portfolio Trading]FTradeRow:rebalanceInput = ref(parent:rebalanceInput);