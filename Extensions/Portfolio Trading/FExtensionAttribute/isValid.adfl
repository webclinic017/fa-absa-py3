[Portfolio Trading]FFuture:isValid = select(payType, 
  "Future"->select(rollContractChoices.IsEmpty, 
    true->false, 
    default->true), 
  default->false);
[Portfolio Trading]FInstrument:isValid = false;
[Portfolio Trading]FMultiInstrumentAndTrades:isValid = false;
[Portfolio Trading]FSingleInstrumentAndTrades:isValid = instrument:isValid;