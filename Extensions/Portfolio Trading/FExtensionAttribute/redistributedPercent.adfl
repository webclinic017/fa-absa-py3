[Portfolio Trading]FInstrumentAndTrades:redistributedPercent = switch(parent:childrenHasInput [settings, targetColumnSetExternally, distributionTypeSetExternally], 
  true->float(((parent:sumOfDiffFromParent [settings, targetColumnSetExternally, distributionTypeSetExternally] * unchangedWeightChangePercent) / object :* getExtensionAttribute) * 100), 
  default->0);