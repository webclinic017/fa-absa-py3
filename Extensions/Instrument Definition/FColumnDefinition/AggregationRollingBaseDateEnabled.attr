[Instrument Definition]FFrame:AggregationRollingBaseDateEnabled =
  ColumnName=Use compounding Rolling Base Day
  Description=If selected reset compounding dates, for all cash flows, will be rolled (using the ‘Comp Period’) from a user defined single fixed Rolling Base Day. For the currently selected reset type the calculated compounding dates are not persisted (as opposed to the reset start and end dates used for the weighting).
  GroupLabel=

