[Instrument Definition]CInsDef_DIVIDEND_POINT_INDEX:IncludeTaxFactor =
  ColumnName=Tax Handling
  Description=Determines if dividends are passed including tax factors (Implicit) or not (None). If set to 'Explicit' the handling is defined by adding Tax Factor resets on Dividend cash flows.


[Instrument Definition]CInsDef_TOTAL_RETURN_SWAP:IncludeTaxFactor =
  ColumnName=Tax Handling
  Description=Determines if dividends are passed including tax factors (Implicit) or not (None). If set to 'Explicit' the handling is defined by adding Tax Factor resets on Dividend cash flows.

