[Core Cash Flow Pricing]FMoneyFlow:acquireDayOrSpotDate = select(isValidDateTime(trade.AcquireDay), 
  true->trade.AcquireDay, 
  false->instrumentSpotDate);
[Core Cash Flow Pricing]FMoneyFlowAndTrades:acquireDayOrSpotDate = select(isValidDateTime(trade.AcquireDay), 
  true->trade.AcquireDay, 
  false->instrumentSpotDate);