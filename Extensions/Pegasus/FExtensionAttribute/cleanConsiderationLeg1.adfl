[Pegasus]FTradeRow:cleanConsiderationLeg1 = select(object.Instrument.InsType, 
  "BuySellBack"->-1 * (py("cleanPrice", context).cleanConsiderationLeg1(object) * (underlying:nominal)) * object.Trade.Quantity, 
  default->0.0);