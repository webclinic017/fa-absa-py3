[Pegasus]FTradeRow:cleanConsiderationLeg2 = select(object.Instrument.InsType, 
  "BuySellBack"->(py("cleanPrice", context).cleanConsiderationLeg2(object) * (underlying:nominal)) * object.Trade.Quantity, 
  default->0.0);