#grouping: risk factors/trade inclusion datetime

[Profit And Loss Cut-Off Time]FInstrument:tradeInclusionDateTime = select(pLCutOffTime, 
  true->date(valuationDateTime) + plCutOffTimeAdjustment(object), 
  false->notADateTime);