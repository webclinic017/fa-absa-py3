[Collateral Margin Report]FInstrumentAndTrades:counterpartyGroupingReference = select(nil == grouping, 
  true->nil, 
  false->value(grouping.GroupingValueAtGrouper(counterpartyGrouper)));
[Collateral Margin Report]FPortfolioInstrumentAndTrades:counterpartyGroupingReference = nil;