[Treasury Pricing and Risk]FInstrumentAndTrades:atmMalzVol = select(count(uniqueVolatilityInformation), 
  1->select(usesMalzVol, 
    true->uniqueVolatilityInformation.Value(expiryDate), 
    false->nil), 
  default->nil);