[Treasury Pricing and Risk]FInstrumentAndTrades:instrument2 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument2);
[Treasury Pricing and Risk]FTimeBucketAndObject:instrument2 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument2);