[Treasury Pricing and Risk]FInstrumentAndTrades:instrument1 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument1);
[Treasury Pricing and Risk]FTimeBucketAndObject:instrument1 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument1);