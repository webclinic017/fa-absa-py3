[Treasury Pricing and Risk]FFxSwap:currency1 = select(currencyPair, 
  nil->insRecLeg:currency, 
  default->currencyPair.Currency1);
[Treasury Pricing and Risk]FInstrumentAndTrades:currency1 = select(currencyPair, 
  nil->nil, 
  default->currencyPair.Currency1);
[Treasury Pricing and Risk]FOdf:currency1 = object.GetCurrencyOne;
[Treasury Pricing and Risk]FTimeBucketAndObject:currency1 = select(currencyPair, 
  nil->nil, 
  default->currencyPair.Currency1);