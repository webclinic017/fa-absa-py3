[Treasury Pricing and Risk]FInstrumentAndTrades:instrumentPair = select(currencyPair, 
  nil->preciousMetalPair, 
  default->currencyPair);
[Treasury Pricing and Risk]FTimeBucketAndObject:instrumentPair = rowObject:instrumentPair;