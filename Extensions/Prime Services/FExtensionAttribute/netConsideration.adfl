[Prime Services]FInstrumentAndTrades:netConsideration = select(originalPremium <> nil, 
  true->double(dailyVat) + double(dailyExecutionFeeNoVat) + double(securitiesTransferTax) + double(originalPremium), 
  false->nil);