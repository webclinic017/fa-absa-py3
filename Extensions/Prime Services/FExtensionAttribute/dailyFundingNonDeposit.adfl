[Prime Services]FSingleInstrumentAndTrades:dailyFundingNonDeposit = select(allFundingPortfolios, 
  true->select(fundingPortfolioType, 
    "CFD"->-1 * py("PS_FundingCalculations", context).CalculateFunding(cfdUnderlying, fundingInstruments, businessDayBeforePnLEndDate, profitAndLossEndDate, validTradesInPosition), 
    default->py("PS_FundingCalculations", context).CalculateFunding(instrument, fundingInstruments, businessDayBeforePnLEndDate, profitAndLossEndDate, validTradesInPosition)), 
  false->0, 
  default->0);