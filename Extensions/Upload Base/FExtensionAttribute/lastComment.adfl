[Upload Base]FBusinessProcess:lastComment = shunt(notes.Size > 0, 
  true->notes.Last, 
  default->nil);
[Upload Base]FBusinessProcessMultiItem:lastComment = shunt(notes.Size > 0, 
  true->notes.Last, 
  default->nil);