[Fixed Income Pricing and Risk]FDeposit:parCoupon = select(count(floatLegs) == 0, 
  true->parRate, 
  false->parSpread);
[Fixed Income Pricing and Risk]FInstrument:parCoupon = select(isSpreadQuoted, 
  false->parRate, 
  true->parSpread);