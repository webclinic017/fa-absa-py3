[Fixed Income Pricing and Risk]FLeg:correlationInformationForLeg = select(usesCorrelationMatrix, 
  true->select(object.MappedCorrelationLink, 
    nil->nil, 
    default->correlationMatrixInformation), 
  false->nil);