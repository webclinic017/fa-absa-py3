[Fixed Income Pricing and Risk]FLeg:usesQuantoCorrelationMatrix = select(forwardCurveForLeg, 
  nil->false, 
  default->select(staticLegInformation.IsQuanto, 
    true->usesCorrelationMatrix, 
    default->false));