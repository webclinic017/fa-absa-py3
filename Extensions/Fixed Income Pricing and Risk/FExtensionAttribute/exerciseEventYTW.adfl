[Fixed Income Pricing and Risk]FExerciseEvent:exerciseEventYTW = switch(marketYieldToWorst, 
  nil->nil, 
  default->(marketYieldToWorst).Second);
[Fixed Income Pricing and Risk]FInstrument:exerciseEventYTW = switch(marketYieldToWorst, 
  nil->nil, 
  default->(instrument:marketYieldToWorst).Second);