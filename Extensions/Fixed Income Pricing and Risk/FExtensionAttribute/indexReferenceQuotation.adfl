[Fixed Income Pricing and Risk]FLeg:indexReferenceQuotation = select(priceInterpretationType, 
  "All In"->getObject("FQuotation", "Pct of Nominal"), 
  default->select(indexReference, 
    nil->nil, 
    default->indexReference:quotation));