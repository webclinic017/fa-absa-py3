[Fixed Income Pricing and Risk]FCashFlowInformation:fraCouponFactor = -fraFactor * object.Period * nominal;
[Fixed Income Pricing and Risk]FLeg:fraCouponFactor = select(firstCashFlow, 
  nil->denominatedvalue(0), 
  default->discountCurve.DiscountValue(firstCashFlow :* "fraCouponFactor" [legInformation], forwardValueDate));