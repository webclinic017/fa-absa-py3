[Fixed Income Pricing and Risk]FLeg:triggerReferenceForwardValueProvider = select(triggerReference, 
  nil->nil, 
  default->triggerReference :* "referenceProvider" [priceSource, doSplitAll, discountingType]);