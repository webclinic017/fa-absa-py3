[Securities Lending]FInstrument:suggestedFeeInterestAttributionDate = select(inclusionMethod, 
  "Following"->max(firstSpreadFixingDate, valuationDate), 
  default->dateAddDelta(max(firstSpreadFixingDate, valuationDate), 0, 0, 1));