[T3 Allocations]FInstrumentAndTrades:settleDaysOffset = select(grouper, 
  nil->nil, 
  default->select(grouper.DisplayName.Contains("SettleDaysOffset"), 
    true->groupingValue, 
    default->nil));
[T3 Allocations]FTradeRow:settleDaysOffset = trade.SettleDaysOffset;