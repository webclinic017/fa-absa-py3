#grouping: default/market making

[Equity Market Making]FQuoteController:qPriorityTick = switch(priceSource, 
  "ADS"->0.01, 
  default->select(useUnderlyingPriceQuoting, 
    false->0.01, 
    true->select(qUnderlyingPriceFeed, 
      nil->0.01, 
      default->qUnderlyingPriceFeed :* "tickSize" [tickSizePrice = underlyingPriceNodeInTheoreticalPrice])));