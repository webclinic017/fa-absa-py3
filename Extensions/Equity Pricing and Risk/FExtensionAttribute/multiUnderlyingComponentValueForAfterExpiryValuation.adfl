[Equity Pricing and Risk]FInstrument:multiUnderlyingComponentValueForAfterExpiryValuation = select(isCashSettled, 
  true->shunt[priceSource, expiryDate](pricingPriceFeed.MtMPrice(expiryDate).PriceExists, 
    true->stripDateTimePart(pricingPriceFeed.MtMPrice(expiryDate).Get), 
    false->marketSpotValue), 
  false->marketSpotValue);