[Equity Pricing and Risk]FInstrument:forwardStartStrikeFactor = nil;
[Equity Pricing and Risk]FOption:forwardStartStrikeFactor = select(isCliquet, 
  true->select(cliquetSimple, 
    false->forwardStartStrikeAlpha * forwardStartForwardFactor, 
    true->cliquetAlphaFromRelativeSpotPercent), 
  false->nil);