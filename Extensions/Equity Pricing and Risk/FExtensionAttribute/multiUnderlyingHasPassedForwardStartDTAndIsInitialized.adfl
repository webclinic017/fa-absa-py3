[Equity Pricing and Risk]FOption:multiUnderlyingHasPassedForwardStartDTAndIsInitialized = select(isPerformancePayoff and (count(multiUnderlyingInitialFixingsStored) == 0), 
  true->throwError("No initial fixings defined for performance option."), 
  default->(forwardStartDateTime <= valuationDateTime) or not (hasInvalidMultiUnderlyingInitialFixingToday));