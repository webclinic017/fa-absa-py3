[Equity Pricing and Risk]FOption:ladderNonReachedRungValue = select(count(ladderNonReachedRungLevels) > 0, 
  false->nil, 
  true->arrayAny(object:barrierValueModel [barrierStored = lot(ladderNonReachedRungLevels), isBarrierOption = true, priceSource, underlyingFutureObservationDate, calculationCurrency, ctdFactor, doSplitAll]));