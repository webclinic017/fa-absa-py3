[Equity Pricing and Risk]FInstrument:includeTaxFactor = true;
[Equity Pricing and Risk]FLeg:includeTaxFactor = select(passingData, 
  nil->true, 
  default->select(passingData.IncludeTaxFactor, 
    "Implicit"->true, 
    default->false));