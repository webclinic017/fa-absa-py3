[Equity Pricing and Risk]FInstrument:averageStartDate = nil;
[Equity Pricing and Risk]FOption:averageStartDate = select(isAsian, 
  true->select(asianNumberAveragePriceDates > 0, 
    true->asianUsedAveragePriceDates[0], 
    false->nil), 
  false->nil);