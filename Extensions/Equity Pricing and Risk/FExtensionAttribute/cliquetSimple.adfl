[Equity Pricing and Risk]FInstrument:cliquetSimple = select(cliquetType, 
  "Pay End"->true, 
  "Pay Go"->true, 
  default->false);
[Equity Pricing and Risk]FOption:cliquetSimple = switch(cliquetType, 
  "Pay Go"->true, 
  "Pay End"->true, 
  default->false);