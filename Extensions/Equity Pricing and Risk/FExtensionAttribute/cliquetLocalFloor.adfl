[Equity Pricing and Risk]FInstrument:cliquetLocalFloor = select(cliquetLocalFloorPercent, 
  nil->nil, 
  default->cliquetLocalFloorPercent / 100.0);
[Equity Pricing and Risk]FOption:cliquetLocalFloor = switch(cliquetSimple, 
  false->exotic.LocalFloor / 100.0, 
  true->nil);