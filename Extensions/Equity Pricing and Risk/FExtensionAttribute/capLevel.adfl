[Equity Pricing and Risk]FVarianceSwap:capLevel = select(useCapStrike, 
  true->pow(variationSwapCap, 2), 
  false->0.0);
[Equity Pricing and Risk]FVolatilityFuture:capLevel = select(useCapStrike, 
  true->variationSwapCap, 
  false->0.0);