[Equity Pricing and Risk]FInstrument:averageEndDate = nil;
[Equity Pricing and Risk]FOption:averageEndDate = select(isAsian, 
  true->select(asianNumberAveragePriceDates > 0, 
    true->asianUsedAveragePriceDates[asianNumberAveragePriceDates - 1], 
    false->nil), 
  false->nil);