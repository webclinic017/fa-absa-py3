[Equity Pricing and Risk]FInstrument:averageNextDate = nil;
[Equity Pricing and Risk]FOption:averageNextDate = select(isAsian, 
  true->select(asianNumberFutureAveragePriceDates > 0, 
    true->asianUsedFutureAveragePriceDates[0], 
    false->nil), 
  false->nil);