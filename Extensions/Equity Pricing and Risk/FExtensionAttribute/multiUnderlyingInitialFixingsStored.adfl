[Equity Pricing and Risk]FInstrument:multiUnderlyingInitialFixingsStored = select(count(priceFixingEventsStored) > 0, 
  true->eventsInPeriod(priceFixingEventsStored, notADateTime, forwardStartDateTime), 
  false->nil);