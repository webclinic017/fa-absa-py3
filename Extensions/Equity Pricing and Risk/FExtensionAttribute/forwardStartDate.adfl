[Equity Pricing and Risk]FOption:forwardStartDate = select(isForwardStart, 
  true->date(forwardStartDateTime), 
  false->nil);
[Equity Pricing and Risk]FVolatilityFuture:forwardStartDate = variationSwapFirstFixingDate;