[Equity Pricing and Risk]FInstrument:cliquetGlobalCap = select(cliquetGlobalCapPercent, 
  nil->nil, 
  default->cliquetGlobalCapPercent / 100.0);
[Equity Pricing and Risk]FOption:cliquetGlobalCap = switch(cliquetSimple, 
  false->exotic.GlobalCap / 100.0, 
  true->nil);