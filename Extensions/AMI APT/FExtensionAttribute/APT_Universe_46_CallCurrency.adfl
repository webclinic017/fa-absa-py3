[AMI APT]FInstrument:APT_Universe_46_CallCurrency = nil;
[AMI APT]FOption:APT_Universe_46_CallCurrency = select(isFXOption, 
  true->select(isCall, 
    true->object.Underlying, 
    default->object.StrikeCurrency));