[PreAndPostTradeAnalysis]FSalesOrder:postTradePerformanceAbsCustomerCurrency = switch((initCustFxRate > 0) and (customerCurrency <> nil), 
  true->denominatedvalue(double(postTradePerformanceAbs) * initCustFxRate, customerCurrency, , ), 
  default->0);