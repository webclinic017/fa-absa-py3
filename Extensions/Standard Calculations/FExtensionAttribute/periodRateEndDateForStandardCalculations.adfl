[Standard Calculations]FCashFlowMoneyFlow:periodRateEndDateForStandardCalculations = select(valuationDate > startDate, 
  true->min(valuationDate, endDate), 
  default->endDate);
[Standard Calculations]FMoneyFlowAndTrades:periodRateEndDateForStandardCalculations = select(valuationDate > startDate, 
  true->min(valuationDate, endDate), 
  default->endDate);