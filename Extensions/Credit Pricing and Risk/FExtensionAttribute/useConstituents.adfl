[Credit Pricing and Risk]FObject:useConstituents = select(instrument:indexAsSumOfConstituents, 
  "true"->true, 
  "false"->false, 
  default->nilObject:useConstituents);
[Credit Pricing and Risk]FUndefinedObject:useConstituents = check(valuationParameters.IndexAsSumOfConstituents);