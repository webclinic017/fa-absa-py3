[Credit Pricing and Risk]FInstrument:instrumentCorrelation = select(correlationMatrix, 
  nil->0, 
  default->select(correlationMatrix.Type, 
    "Instrument Correlation"->instrumentCorrelationMatrixInformation.Correlation(object), 
    default->0));