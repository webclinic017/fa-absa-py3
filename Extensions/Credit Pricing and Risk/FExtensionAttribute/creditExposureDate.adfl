[Credit Pricing and Risk]FInstrumentAndTrades:creditExposureDate = select(timeBucketsSetExternally, 
  nil->valuationDate, 
  default->date(timeBucketsSetExternally :* "max(valuationDate, startDate)"));