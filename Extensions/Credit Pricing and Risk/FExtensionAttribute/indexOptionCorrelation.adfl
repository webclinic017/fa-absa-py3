[Credit Pricing and Risk]FInstrument:indexOptionCorrelation = select(correlationMatrix, 
  nil->0, 
  default->select(correlationMatrix.Type, 
    "Base Correlation"->lowerBaseCorrelationValue, 
    "Correlation Matrix"->issuerCorrelations, 
    "Instrument Correlation"->instrumentCorrelation, 
    default->0));