[Deal Sales Trading Interaction]FGroupingOrder:hitRate = switch(bool(totalNumberOfCustomerRequests), 
  true->isTraded / totalNumberOfCustomerRequests, 
  false->nil);
[Deal Sales Trading Interaction]FMultiQuoteRequestRow:hitRate = switch(bool(totalNumberOfCustomerRequests), 
  true->isTraded / totalNumberOfCustomerRequests, 
  false->nil);