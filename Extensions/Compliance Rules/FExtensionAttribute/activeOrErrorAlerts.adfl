[Compliance Rules]FAlert:activeOrErrorAlerts = switch(object.State, 
  "Active"->object, 
  "Error"->object, 
  default->nil);
[Compliance Rules]FComplianceRule:activeOrErrorAlerts = flattenAndRemoveNil((alerts) :* "activeOrErrorAlerts");