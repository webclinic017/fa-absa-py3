[Automated Trading]ExecutionAlgo:algoCurrentOrderCanBeModified = switch(currentOrderActive, 
  false->false, 
  default->((currentOrderFillRatio >= modificationFillLimit) or (currentOrder.FilledQuantity("Order") == 0))) or (trigger == "MatchAll");