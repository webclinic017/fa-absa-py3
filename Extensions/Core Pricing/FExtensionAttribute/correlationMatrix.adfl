[Core Pricing]FCorrelationMatrix:correlationMatrix = object;
[Core Pricing]FInstrument:correlationMatrix = select(mappedCorrelationLink, 
  nil->nil, 
  default->mappedCorrelationLink.Link(liveOrSnapshotParameterDate));
[Core Pricing]FLeg:correlationMatrix = select(mappedCorrelationLink, 
  nil->nil, 
  default->mappedCorrelationLink.Link(liveOrSnapshotParameterDate));