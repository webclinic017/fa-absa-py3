[Core Pricing]FInstrumentAndTrades:groupingSpotDiscountDate = select(useGroupingSpot, 
  true->select(spotGroupingReference, 
    nil->nil, 
    default->collapse(groupingSpotDate)), 
  false->nil);
[Core Pricing]FTimeBucketAndObject:groupingSpotDiscountDate = rowInstrumentAndTrades:groupingSpotDiscountDate;