[Core Pricing]FInstrumentAndTrades:displayDate = select(useGroupingSpot, 
  true->select(spotGroupingReference, 
    nil->valuationDate, 
    default->collapse(groupingSpotDate)), 
  false->valuationDate);
[Core Pricing]FTimeBucketAndObject:displayDate = rowInstrumentAndTrades:displayDate;