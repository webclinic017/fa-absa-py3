[Core Pricing]FInstrumentAndTrades:isOptionBreakEvenValueValid = select(isFinite(theoreticalInstrumentSpotValueForPriceInStrikeCurrency) and (1 == countAny(theoreticalInstrumentSpotValueForPriceInStrikeCurrency)) and (1 == countAny(strikeValueForBreakEvenValue)) and not instrument:isAsian and not instrument:isForwardStarting, 
  false->false, 
  true->select(optionCallOrPutFactor, 
    default->true, 
    nil->false));