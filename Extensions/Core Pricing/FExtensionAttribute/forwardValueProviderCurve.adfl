#grouping: risk factors/repo component

[Core Pricing]FInstrument:forwardValueProviderCurve = ref(select(useExplicitRepoCurve, 
  true->repoCurve, 
  false->domesticCurrencyMoneyMarketCurve), object);