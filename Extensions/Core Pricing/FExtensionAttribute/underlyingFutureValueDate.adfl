[Core Pricing]FInstrument:underlyingFutureValueDate = select(count(underlyingFutureValueDates), 
  0->throwError("Underlying Future selection is empty."), 
  1->underlyingFutureValueDates[0], 
  default->throwError("Non unique value date not supported for underlying future method"));