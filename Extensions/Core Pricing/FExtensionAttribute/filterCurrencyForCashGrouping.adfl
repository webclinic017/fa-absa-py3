[Core Pricing]FInstrumentAndTrades:filterCurrencyForCashGrouping = select((not calculateCash) and (cashGrouperFilterCurrency <> nil), 
  true->cashGrouperFilterCurrency, 
  false->collapse(globalFilterCurrency));