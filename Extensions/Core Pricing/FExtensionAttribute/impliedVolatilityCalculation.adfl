[Core Pricing]FInstrument:impliedVolatilityCalculation = shunt(implyValid and (exerciseEventCount < 2), 
  false->nil, 
  true->imply(volatilityInTheoreticalPrice, theoreticalPrice, marketPrice, impliedVolatilityAccuracy, impliedVolatilityMinValue, impliedVolatilityMaxValue, impliedVolatilityMaxIterations));