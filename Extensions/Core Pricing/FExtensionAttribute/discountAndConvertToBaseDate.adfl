[Core Pricing]FInstrument:discountAndConvertToBaseDate = select(nil == groupingSpotDiscountDate, 
  true->select(valuesOnSpot, 
    true->instrumentSpotOrStartDate, 
    false->valuationDate), 
  false->groupingSpotDiscountDate);