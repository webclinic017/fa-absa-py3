[Core Pricing]FMoneyFlow:displayOrSpotDate = select(valuesOnSpot, 
  true->instrumentSpotOrStartDate, 
  false->displayDate);
[Core Pricing]FMoneyFlowAndTrades:displayOrSpotDate = select(valuesOnSpot, 
  true->instrumentSpotOrStartDate, 
  false->displayDate);