[Core Pricing]FInstrument:carryCostAdjustedQuanto = select(isQuanto, 
  true->select(baseValuationOnUnderlyingComponents, 
    true->carryCostAdjusted, 
    false->quantoAdjustedCarryCost(carryCostAdjusted, underlyingFXCorrelation, volatilityForQuantoAdjustedCarryCost, fxVolatilityForQuantoAdjustedCarryCost)), 
  false->carryCostAdjusted);