[Core Pricing]FInstrument:useUnderlyingFutureProvider = select(snoop01(theoreticalPrice, "underlyingFutureValueProvider"), 
  default->true, 
  nil->false);
[Core Pricing]FInstrumentAndTrades:useUnderlyingFutureProvider = select(snoop01(theoreticalPrice, "underlyingFutureValueProvider", alwaysMatchedFilter, lotIndex), 
  default->true, 
  nil->false);
[Core Pricing]FPriceAggregate:useUnderlyingFutureProvider = select(snoop01(theoreticalPrice, "underlyingFutureValueProvider"), 
  default->true, 
  nil->false);