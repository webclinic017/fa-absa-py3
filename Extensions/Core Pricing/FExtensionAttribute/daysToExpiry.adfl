[Core Pricing]FInstrument:daysToExpiry = select(useFullDayClock, 
  true->dateDifference(expiryDateTime, valuationDateTime), 
  false->expiryDateTime - valuationDateTime);
[Core Pricing]FVolatilityPoint:daysToExpiry = select(useFullDayClock, 
  true->dateDifference(expiryDateTime, valuationDateTime), 
  false->expiryDateTime - valuationDateTime);