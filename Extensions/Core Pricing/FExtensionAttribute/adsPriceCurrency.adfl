[Core Pricing]FFxSwap:adsPriceCurrency = adsPriceCurrency = select(globalCurrency, 
  nil->currency, 
  default->globalCurrency);
[Core Pricing]FObject:adsPriceCurrency = select(globalCurrency, 
  nil->nilObject:adsPriceCurrency, 
  default->globalCurrency);
[Core Pricing]FUndefinedObject:adsPriceCurrency = var(nil, "FCurrency");