[Core Pricing]FTimeBucket:timeBucketIsInceptionToEndBucket = (startDate == smallDate) and (endDate == bigDate);
[Core Pricing]FTimeBucketAndObject:timeBucketIsInceptionToEndBucket = (startDate == smallDate) and (endDate == bigDate);