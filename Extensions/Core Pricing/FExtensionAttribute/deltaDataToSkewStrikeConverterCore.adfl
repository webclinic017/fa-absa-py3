[Core Pricing]FObject:deltaDataToSkewStrikeConverterCore[spotDelta, instrumentForDeltaConverter, priceSource, doSplitAll, exposeForwardValuationTargets] = select(spotDelta, 
  true->collapse(spotToForwardDeltaConverter), 
  false->nil);