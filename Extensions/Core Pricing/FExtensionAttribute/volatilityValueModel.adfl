#grouping: greek perimeters/price greek perimeters

[Core Pricing]FInstrument:volatilityValueModel = select(needVolConversion, 
  true->volYield2PriceFactor * volatility, 
  false->collapse(volatilityNormalized));