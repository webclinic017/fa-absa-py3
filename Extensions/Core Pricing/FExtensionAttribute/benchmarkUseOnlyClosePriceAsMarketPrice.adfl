[Core Pricing]FInstrument:benchmarkUseOnlyClosePriceAsMarketPrice = select(useMtmForHistoricalDate, 
  true->select(isHistoricalMode, 
    true->true, 
    false->benchmarkPriceFeed.PriceReadDate < valuationSystemDate), 
  false->false);