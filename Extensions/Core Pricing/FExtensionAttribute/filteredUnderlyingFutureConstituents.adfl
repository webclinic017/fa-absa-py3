[Core Pricing]FInstrument:filteredUnderlyingFutureConstituents = select(forwardValuationObservations, 
  nil->underlyingFutureConstituents, 
  default->FilterConstituents(underlyingFutureConstituents, forwardValuationPeriod));