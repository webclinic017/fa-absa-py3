[IRD Pricing and Risk]FInstrument:lastExerciseEvent = select(hasExerciseEvents, 
  true->maxObj(exerciseEventArray, "ExpiryDate"), 
  default->nil);
[IRD Pricing and Risk]FInstrumentAndTrades:lastExerciseEvent = instrument:lastExerciseEvent;