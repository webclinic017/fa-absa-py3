[Profit and Loss]FTradingSheet:Portfolio Trade Filter Match Choice =
  Description=When using a trade filter, this functionality decides whether to match trades within the entire trade filter or only within physical portfolios.
  ExtensionAttribute=tradeFilterMatchChoice
  Format=enum(TradeFilterMatchChoice)
  GroupLabel=Profit/Loss
  LabelList=Trade Filter Match Choice
  Name=Trade Filter Match Choice
  Save=Enabled
  SettingScope=Global
  ValueDomain=enum(TradeFilterMatchChoice)

