[Profit and Loss]FObject:breakEvenRateImpl = select(instrumentPair == nil, 
  true->nil, 
  false->switch(double(projectedCashInventoryCurrencies[0]) * double(projectedCashInventoryCurrencies[1]) >= 0.0, 
    true->nil, 
    default->-(projectedCashInventoryCurrencies[1] / (projectedCashInventoryCurrencies[0] * instrument1:contractSizeInQuotation))));