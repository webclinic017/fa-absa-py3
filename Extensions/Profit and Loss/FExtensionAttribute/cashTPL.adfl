[Profit and Loss]FInstrumentAndTrades:cashTPL = nil;
[Profit and Loss]FLegAndTrades:cashTPL = nil;
[Profit and Loss]FMultiInstrumentAndTrades:cashTPL = select(calculateCash, 
  true->cashUPL + cashRPL + cashAccruedIncome + cashSettledIncome, 
  false->plZero);