[Profit and Loss]FInstrument:profitAndLossFXRateDate = select(pLPeriodFX, 
  "Current Date"->valuationSystemDate, 
  "Start Date"->profitAndLossStartDate, 
  "End Date"->profitAndLossEndDate, 
  default->valuationDate);
[Profit and Loss]FInstrumentAndTrades:profitAndLossFXRateDate = select(pLPeriodFX, 
  "Current Date"->valuationSystemDate, 
  "Start Date"->profitAndLossStartDate, 
  "End Date"->profitAndLossEndDate, 
  default->valuationDate);