[Profit and Loss]FSingleInstrumentAndTrades:meanCleanValue = switch(abs(meanValuePosition) > 1e-06, 
  true->setDateTimePart(cleanTotalValue / meanValuePosition, profitAndLossEndDateAdjusted), 
  false->nil);