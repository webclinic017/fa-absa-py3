[Profit and Loss]FInstrumentAndTrades:fundPortfolioRow = select(fundInstruments.Size > 0, 
  false->nil, 
  default->parent:fundPortfolioRow);
[Profit and Loss]FPortfolioInstrumentAndTrades:fundPortfolioRow = select(parent, 
  nil->object, 
  default->parent:fundPortfolioRow);