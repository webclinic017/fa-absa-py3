[WSO Bank Debt]FObject:wSOUploadTradeTemplateExternalFACurrencyBkg = select(wSOUploadTradeTemplateExternalFACurrency, 
  default->select(object.IsKindOf("FBusinessProcess"), 
    true->select(object.Subject.Subject, 
      default->switch(py("FReconciliationValueMapping", context).CompareValuePair(wSOUploadTradeTemplateInternalFACurrency, wSOUploadTradeTemplateExternalFACurrency, "string"), 
        true->1, 
        default->0)), 
    default->switch(py("FReconciliationValueMapping", context).CompareValuePair(wSOUploadTradeTemplateInternalFACurrency, wSOUploadTradeTemplateExternalFACurrency, "string"), 
      true->1, 
      default->0)));