[WSO Bank Debt]FObject:wSOUploadFacilityTemplateExternalFACurrencyBkg = select(wSOUploadFacilityTemplateExternalFACurrency, 
  default->select(object.IsKindOf("FBusinessProcess"), 
    true->select(object.Subject.Subject, 
      default->switch(py("FReconciliationValueMapping", context).CompareValuePair(wSOUploadFacilityTemplateInternalFACurrency, wSOUploadFacilityTemplateExternalFACurrency, "string"), 
        true->1, 
        default->0)), 
    default->switch(py("FReconciliationValueMapping", context).CompareValuePair(wSOUploadFacilityTemplateInternalFACurrency, wSOUploadFacilityTemplateExternalFACurrency, "string"), 
      true->1, 
      default->0)));