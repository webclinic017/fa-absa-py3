[UDMCUtilMod]FOption:udmcBarrier2Alpha = select(storedStrikeType, 
  "Rel Spot Pct"->(double(barrier2Stored) + 100) / 100, 
  "Rel Spot Pct 100"->double(barrier2Stored) / 100, 
  default->throwError("Invalid strike type for udmcBarrier2Alpha"));