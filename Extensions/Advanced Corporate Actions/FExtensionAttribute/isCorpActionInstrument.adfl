[Advanced Corporate Actions]FInstrument:isCorpActionInstrument = select(corpActionSetExternally, 
  default->underlyingOrSelf == corpActionSetExternally.Instrument, 
  nil->false);
[Advanced Corporate Actions]FTradeRow:isCorpActionInstrument = select(nextCorpAction, 
  default->instrument:underlyingOrSelf == nextCorpAction.Instrument, 
  nil->false);