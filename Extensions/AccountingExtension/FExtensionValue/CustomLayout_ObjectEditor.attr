[AccountingExtension]FAccountingPeriod:CustomLayout_ObjectEditor
hbox(;
        hbox[Accounting Period;
                vbox(;
                        StartDate;
                        EndDate;
                        FiscalYear;
                );
                vbox(;
                        Status;
                );
                
        ];
);
...
