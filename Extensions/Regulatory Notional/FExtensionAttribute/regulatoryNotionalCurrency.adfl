[Regulatory Notional]FTrade:regulatoryNotionalCurrency = select((collapse(regulatoryNotional) == nil) or (collapse(regulatoryNotional).Unit == nil), 
  true->nil, 
  default->currencyFromUnit(collapse(regulatoryNotional).Unit));