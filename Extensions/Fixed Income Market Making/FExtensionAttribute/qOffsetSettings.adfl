[Fixed Income Market Making]FQuoteController:qOffsetSettings = shunt(qOffsetOrderBook, 
  nil->select(qMultiQuoteController, 
    nil->nil, 
    default->qMultiQuoteController:proposedSpreadData), 
  default->createQuoteSettings(qQuoteContext, qOffsetOrderBook).FixedIncomeSpreadData);
[Fixed Income Market Making]FQuoteSetting:qOffsetSettings = shunt(qOffsetOrderBook, 
  nil->select(qOffsetController, 
    nil->nil, 
    default->qOffsetController:proposedSpreadData), 
  default->createQuoteSettings(qQuoteContext, qOffsetOrderBook).FixedIncomeSpreadData);