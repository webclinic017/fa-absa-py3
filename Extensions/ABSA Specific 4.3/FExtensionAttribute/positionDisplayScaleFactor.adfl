[ABSA Specific 4.3]FOption:positionDisplayScaleFactor[] = select(underlying:isCashFlowInstrument, 
  true->select(underlying.InsType == "Bond", 
    true->1 / contractSize, 
    default->1), 
  false->1 / contractSize);