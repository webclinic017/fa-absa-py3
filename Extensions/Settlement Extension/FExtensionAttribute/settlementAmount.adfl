[Settlement Extension]FSettlement:settlementAmount = object.AmountDV;
[Settlement Extension]FSettlementMultiItem:settlementAmount = switch(count(settlementSum) == 1, 
  true->settlementSum, 
  false->nil);