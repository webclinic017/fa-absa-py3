[Convertible Pricing And Risk]FInstrument:bondOnlyDurationFromBondOnlyModifiedDuration = select(fixLeg, 
  nil->nan, 
  default->select(fixLeg:rollingFrequency, 
    0->(1 + double(bondOnlyYieldToMaturity)) * bondOnlyModifiedDuration, 
    default->(1 + double(bondOnlyYieldToMaturity) / fixLeg:rollingFrequency) * bondOnlyModifiedDuration));