[ABSA Columns]FSingleInstrumentAndTrades:aamDepositPrice = select(instrument.FirstFixedLeg.RollingPeriod, 
  "0d"->100 * theoreticalValue / aamDepositMaturity, 
  default->100 * theoreticalValue / riskPosition);
[ABSA Columns]FTradeRow:aamDepositPrice = select(instrument.FirstFixedLeg.RollingPeriod, 
  "0d"->100 * theoreticalValue / aamDepositMaturity, 
  default->100 * theoreticalValue / riskPosition);