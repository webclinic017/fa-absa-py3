[ABSA Accounting 4.3]FInstrumentAndTrades:equivalentWhiteMazeDelta = double(priceDelta * marketPriceADS) / (double(whiteMaizeLast) * 100);
[ABSA Accounting 4.3]FMultiInstrumentAndTrades:equivalentWhiteMazeDelta = double(posDelta) / (double(whiteMaizeLast) * 100);
[ABSA Accounting 4.3]FTradeRow:equivalentWhiteMazeDelta = select(isExpiredFullDay, 
  true->0, 
  default->double(priceDelta * marketPriceADS) / (double(whiteMaizeLast) * 100));