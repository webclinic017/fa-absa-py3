[Synthetic Prime]FTradingSheet:Calculate Synthetic Columns For All Rows =
  Description=If selected, the Synthetic columns that provide the different components of the portfolio swap performance will calculate values for all rows, i.e. not only for rows reperesenting physical portfolios and their sub-rows, which normally is the case. The columns will then produce an error message on rows where a calculation of portfolio swap performance is not applicable.
  ExtensionAttribute=calculateSyntheticColumnsForAllRows
  GroupLabel=Profit/Loss
  Name=Calculate Synthetic Columns For All Rows
  Save=Enabled
  SettingScope=Global
  ValueDomain=bool

