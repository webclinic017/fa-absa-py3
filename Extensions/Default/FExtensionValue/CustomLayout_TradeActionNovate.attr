[Default]FObject:CustomLayout_TradeActionNovate
hbox(;
    valueDay;
    acquireDay;
);
hbox(;
    closingNominal;
    remainingNominal;
);
hbox(;
    assignedCpy;
    status;
);
stepOut;
vbox[Payments;
    hbox(;
        closingAmount;
        closingPayType;
        closingCpy;
    );
    hbox(;
        assignedAmount;
        assignedPayType;
        assignedPayCpy;
    );
];

...
