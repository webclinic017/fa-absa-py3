[Default]FTradingSheet:Interest Rate Delta Gamma Filter Rule =
  Description=Set whether only the top curve or any curve in a yield curve (spread) hierarchy is applicable to the filter. If not set only top curves (matching the curve filter) are shifted.
  ExtensionAttribute=deltaGammaCriteriaRule
  Name=Layer Filter Rule

