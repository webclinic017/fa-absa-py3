[Default]FInstrumentAndTrades:displayDate = select(useGroupingSpot, 
  true->select(spotGroupingReference, 
    nil->valuationDate, 
    default->collapse(groupingSpotDate)), 
  false->valuationDate);
[Default]FTimeBucketAndObject:displayDate = rowInstrumentAndTrades:displayDate;