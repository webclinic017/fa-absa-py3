[Default]FObject:isHistoricalMode = 
# Generally we do not want to distinguish between hisotrical mode and live mode in ADFL. So use with care (if at all).
nilObject:isHistoricalMode;
[Default]FUndefinedObject:isHistoricalMode = acm.IsHistoricalMode;