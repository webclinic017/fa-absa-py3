#grouping: default/market making

[Default]FOrderBook:qReferencePrice = object.SelectedPrice(qReferencePriceArray).GetPriceForQuantity(object.RoundLot);
[Default]FQuoteController:qReferencePrice = priceFeed.SelectedPrice(qReferencePriceArray).GetPriceForQuantity(qLot);