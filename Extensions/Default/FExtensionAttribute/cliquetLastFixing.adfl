[Default]FOption:cliquetLastFixing = select(isCliquet, 
  true->select(cliquetFirstFixingHasOccurred, 
    true->cliquetFixingsForFutureExpiryInfo[0], 
    false->denominatedvalue(0)), 
  false->nil);