[Default]FInstrumentAndTrades:instrument2 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument2);
[Default]FTimeBucketAndObject:instrument2 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument2);