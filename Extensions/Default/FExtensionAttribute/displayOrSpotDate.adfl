[Default]FMoneyFlow:displayOrSpotDate = select(valuesOnSpot, 
  true->instrumentSpotOrStartDate, 
  false->displayDate);
[Default]FMoneyFlowAndTrades:displayOrSpotDate = select(valuesOnSpot, 
  true->instrumentSpotOrStartDate, 
  false->displayDate);