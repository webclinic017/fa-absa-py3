[Default]FQuoteController:qisDriverValueBid = shunt(quoteParameter <> nil, 
  false->nil, 
  default->shunt(quoteParameter.QuoteInsSpreadFromType(driverTypeFromNameBid).SpreadBid <> nil, 
    false->nil, 
    default->shunt(quoteParameter.QuoteInsSpreadFromType(driverTypeFromNameBid).SpreadBid.IsEqual(nan), 
      true->nil, 
      default->quoteParameter.QuoteInsSpreadFromType(driverTypeFromNameBid).SpreadBid)));