[Default]FInstrumentAndTrades:cashAssetRPL = select(calculateCash, 
  true->select(historicalFXChoice, 
    "None, FX Matching"->assetRPL - cashRPL, 
    default->cashRPL + assetRPL), 
  false->collapse(assetRPL));