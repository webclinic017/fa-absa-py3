[Default]FCashFlowMoneyFlow:unscaledNominalFactor = select(cashFlowInformation, 
  nil->nil, 
  default->select(cashFlowInformation.Nominal(legInformation).Number == 0, 
    true->0.0, 
    default->cashFlowInformation.UnscaledNominal(legInformation) / cashFlowInformation.Nominal(legInformation)));