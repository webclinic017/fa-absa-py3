[Default]FInstrument:needVolConversion = select(object.NeedVolConversion(bondVolType, mappedCoreValuationFunction), 
  true->select(underlying:firstFixedLegInformation, 
    default->true, 
    nil->false), 
  false->false);