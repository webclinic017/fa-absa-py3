[Default]FOrderBook:marketEquilibriumOrPhase = switch(isFinite(marketPriceEquilibrium), 
  false->trading.Status.PhaseAbbreviation, 
  default->marketPriceEquilibrium);
[Default]FTradingInterface:marketEquilibriumOrPhase = switch(isFinite(priceFeed:marketPriceEquilibrium), 
  false->object.Status.PhaseAbbreviation, 
  default->priceFeed:marketPriceEquilibrium);