[Default]FInstrument:ytmCalculationValid = select(isYtmSensitive, 
  true->select(isRepoInstrument, 
    false->select(isExpiredFullDay, 
      false->true, 
      true->false), 
    true->false), 
  false->false);