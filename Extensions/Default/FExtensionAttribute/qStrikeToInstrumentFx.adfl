[Default]FQuoteController:qStrikeToInstrumentFx = select(instrumentCurrency <> currency, 
  true->throwError("Instrument currency is different than the orderbook currency."), 
  default->instrument:fxStrikeToInstrument [priceSource]);