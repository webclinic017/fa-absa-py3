[Default]FInstrumentAndTrades:atmMalzVol = select(count(uniqueVolatilityInformation), 
  1->select(usesMalzVol, 
    true->uniqueVolatilityInformation.Value(expiryDate), 
    false->nil), 
  default->nil);