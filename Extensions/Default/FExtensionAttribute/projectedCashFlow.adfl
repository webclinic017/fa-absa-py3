[Default]FCashFlowInformation:projectedCashFlow = object.RoundedProjectedCashFlow(unRoundedProjectedCashFlow * staticLegInformation:contractSize, valuationDate, nominalForJgb, roundingInformation) / staticLegInformation:contractSize;
[Default]FCashFlowMoneyFlow:projectedCashFlow = select(cashFlowInformation, 
  nil->nil, 
  default->select(cashFlowProjectedFromModelCall, 
    true->collapse(projectedFromModelCall), 
    false->cashFlowInformation :* "projectedCashFlow" [roundingInformation, legInformation = legInformationOrEmpty, unRoundedProjectedCashFlow]) * timeApportionFactor);
[Default]FPayment:projectedCashFlow = denominatedvalue(object.Amount, object.Currency, "Payment", object.PayDay);