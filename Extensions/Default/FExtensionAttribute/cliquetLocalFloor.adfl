[Default]FInstrument:cliquetLocalFloor = select(cliquetLocalFloorPercent, 
  nil->nil, 
  default->cliquetLocalFloorPercent / 100.0);
[Default]FOption:cliquetLocalFloor = switch(cliquetSimple, 
  false->exotic.LocalFloor / 100.0, 
  true->nil);