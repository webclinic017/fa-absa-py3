[Default]FInstrument:ufmMarketSpotValueConstantValueProvider = select((settlementPayType == "Future") or (settlementPayType == "Forward"), 
  true->ConstantValueModel(actualMarketSpotValue), 
  false->throwError("UFM Market only valid for Pay Type Future or Forward."));