[Default]FOrderBook:capabilities = trading:capabilities [capabilitySource];
[Default]FTradingInterface:capabilities = select(capabilitySource, 
  "Home"->homeCapabilities, 
  "Home or Market"->homeOrMarketCapabilities, 
  default->marketCapabilities);