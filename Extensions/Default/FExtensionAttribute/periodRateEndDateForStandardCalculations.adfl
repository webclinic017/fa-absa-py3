[Default]FCashFlowMoneyFlow:periodRateEndDateForStandardCalculations = select(valuationDate > startDate, 
  true->min(valuationDate, endDate), 
  default->endDate);
[Default]FMoneyFlowAndTrades:periodRateEndDateForStandardCalculations = select(valuationDate > startDate, 
  true->min(valuationDate, endDate), 
  default->endDate);