[Default]FGroupingOrder:totalEMQty = collapse(totalEMQtyMemberOrders);
[Default]FInternalMarketOrder:totalEMQty = collapse(totalEMQtyMemberOrders);
[Default]FOrderProgram:totalEMQty = collapse(totalEMQtyMemberOrders);
[Default]FOwnOrder:totalEMQty = switch(isOrderActive, 
  true->object.TotalQuantity, 
  default->0);