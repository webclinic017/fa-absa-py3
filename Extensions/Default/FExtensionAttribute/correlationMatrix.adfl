#grouping: risk factors/correlation component

[Default]FCorrelationMatrix:correlationMatrix = object;
[Default]FInstrument:correlationMatrix = select(mappedCorrelationLink, 
  nil->nil, 
  default->mappedCorrelationLink.Link(liveOrSnapshotParameterDate));
[Default]FLeg:correlationMatrix = select(mappedCorrelationLink, 
  nil->nil, 
  default->mappedCorrelationLink.Link(liveOrSnapshotParameterDate));