[Default]FOption:forwardStartDateTime = select(isForwardStart, 
  true->select(useFullDayClock, 
    true->date(object.ForwardStartDateTime), 
    false->object.ForwardStartDateTime), 
  false->nil);
[Default]FVolatilityFuture:forwardStartDateTime = variationSwapFirstFixingDate;