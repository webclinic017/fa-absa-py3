[Default]FInstrumentAndTrades:instrument1 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument1);
[Default]FTimeBucketAndObject:instrument1 = select(instrumentPair, 
  nil->nil, 
  default->instrumentPair.Instrument1);