[Default]FInstrument:lastExerciseEvent = select(hasExerciseEvents, 
  true->maxObj(exerciseEventArray, "ExpiryDate"), 
  default->nil);
[Default]FInstrumentAndTrades:lastExerciseEvent = instrument:lastExerciseEvent;