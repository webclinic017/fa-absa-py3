[Default]FMoneyFlow:acquireDayOrSpotDate = select(isValidDateTime(trade.AcquireDay), 
  true->trade.AcquireDay, 
  false->instrumentSpotDate);
[Default]FMoneyFlowAndTrades:acquireDayOrSpotDate = select(isValidDateTime(trade.AcquireDay), 
  true->trade.AcquireDay, 
  false->instrumentSpotDate);