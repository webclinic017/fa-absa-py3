[Default]FInstrument:averageStrikeTypeIsFix = false;
[Default]FOption:averageStrikeTypeIsFix = select(object.IsAsian, 
  true->select(averageStrikeType, 
    Fix->true, 
    default->false), 
  false->false);