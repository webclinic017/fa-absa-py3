[Default]QuoteAndHedgeBasketSpread:quoteAndHedgeDonePriceRatio = switch((hedgeLegAveragePrice > 0) and (quoteAndHedgePriceSpread == 0), 
  true->switch(priceRatioType, 
    "Quote Price / Hedge Price"->quoteLegAveragePrice / (hedgeLegAveragePrice * fxRateFromHedgeToQuote), 
    default->(hedgeLegAveragePrice * fxRateFromHedgeToQuote) / quoteLegAveragePrice), 
  default->nan);
[Default]QuoteAndHedgeDeltaNeutral:quoteAndHedgeDonePriceRatio = switch((hedgeLegAveragePrice > 0) and (quoteAndHedgePriceSpread == 0), 
  true->switch(priceRatioType, 
    "Quote Price / Hedge Price"->quoteLegAveragePrice / (hedgeLegAveragePrice * fxRateFromHedgeToQuote), 
    default->(hedgeLegAveragePrice * fxRateFromHedgeToQuote) / quoteLegAveragePrice), 
  default->nan);
[Default]QuoteAndHedgePriceSpread:quoteAndHedgeDonePriceRatio = switch((hedgeLegAveragePrice > 0) and (quoteAndHedgePriceSpread == 0), 
  true->switch(priceRatioType, 
    "Quote Price / Hedge Price"->quoteLegAveragePrice / (hedgeLegAveragePrice * fxRateFromHedgeToQuote), 
    default->(hedgeLegAveragePrice * fxRateFromHedgeToQuote) / quoteLegAveragePrice), 
  default->nan);
[Default]QuoteAndHedgeRightIssue:quoteAndHedgeDonePriceRatio = switch((hedgeLegAveragePrice > 0) and (quoteAndHedgePriceSpread == 0), 
  true->switch(priceRatioType, 
    "Quote Price / Hedge Price"->quoteLegAveragePrice / (hedgeLegAveragePrice * fxRateFromHedgeToQuote), 
    default->(hedgeLegAveragePrice * fxRateFromHedgeToQuote) / quoteLegAveragePrice), 
  default->nan);