[Default]FLeg:uniqueSpread = select((object.PayLeg or ("FRN" == instrument:instrumentType) or ("Deposit" == instrument:instrumentType)) and instrument:isGenericSpreadQuoted, 
  true->0, 
  false->object.UniqueSpread);