[Default]FInstrument:usesDeferredModel = select(isGeneric, 
  true->select(priceBase, 
    "Market"->false, 
    default->select(settlementPayType, 
      "Forward"->false, 
      "Future"->true, 
      default->false)), 
  false->false);