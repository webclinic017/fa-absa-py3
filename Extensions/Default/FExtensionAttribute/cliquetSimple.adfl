[Default]FInstrument:cliquetSimple = select(cliquetType, 
  "Pay End"->true, 
  "Pay Go"->true, 
  default->false);
[Default]FOption:cliquetSimple = switch(cliquetType, 
  "Pay Go"->true, 
  "Pay End"->true, 
  default->false);