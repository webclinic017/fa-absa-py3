[Default]FLeg:floatReferenceQuotation = select(priceInterpretationType, 
  "All In"->getObject("FQuotation", "Pct of Nominal"), 
  default->select(floatRateReference, 
    nil->nil, 
    default->floatRateReference:quotation));