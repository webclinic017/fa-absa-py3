[Default]FInstrument:fxCorrelation = select(correlationMatrix, 
  nil->throwError("FX correlation matrix is missing."), 
  default->select(optionParent, 
    nil->nil, 
    default->fxCorrelationInformation.Correlation));