#grouping: greek perimeters/price greek perimeters
#grouping: risk factors/volatility value

[Default]FOption:rangeAccrualCapVolatility = select(isForwardStarting and useForwardVolatility, 
  default->arrayAny(rangeAccrualCapSpotToExpiryVolatility), 
  true->arrayAny(rangeAccrualForwardStartCapVolatility));