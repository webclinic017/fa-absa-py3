[Default]FInstrument:multiUnderlyingStrikeValueForwardStartPerformance = select(storedStrikeType, 
  "Rel Spot Pct"->denominatedvalue((double(storedStrike) + 100) / 100.0, unit(storedStrike), "Absolute", ), 
  "Rel Spot Pct 100"->denominatedvalue(double(storedStrike) / 100.0, unit(storedStrike), "Absolute", ), 
  default->throwError("Invalid strike type for forward start performance options."));