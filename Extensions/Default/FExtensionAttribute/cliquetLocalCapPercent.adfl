[Default]FInstrument:cliquetLocalCapPercent = nil;
[Default]FInstrumentAndTrades:cliquetLocalCapPercent = instrument:cliquetLocalCapPercent;
[Default]FMultiInstrumentAndTrades:cliquetLocalCapPercent = nil;
[Default]FOption:cliquetLocalCapPercent = select(isCliquet, 
  true->switch(cliquetIsLocalCap, 
    false->nil, 
    true->exotic.LocalCap), 
  false->nil);
[Default]FPriceAggregate:cliquetLocalCapPercent = instrument:cliquetLocalCapPercent;