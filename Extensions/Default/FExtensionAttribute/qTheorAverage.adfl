#grouping: default/market making

[Default]FQuoteController:qTheorAverage = select(instrument:useFIQuoting, 
  true->delta(qTheorAverageNoOffset, 0), 
  false->delta(qTheorAverageNoOffset, qTheorOffset + qReactOnHitOffset));