[Default]FInstrument:daysToExpiry = select(useFullDayClock, 
  true->dateDifference(expiryDateTime, valuationDateTime), 
  false->expiryDateTime - valuationDateTime);
[Default]FVolatilityPoint:daysToExpiry = select(useFullDayClock, 
  true->dateDifference(expiryDateTime, valuationDateTime), 
  false->expiryDateTime - valuationDateTime);