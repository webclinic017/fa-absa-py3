[Default]FInstrumentAndTrades:groupingSpotDiscountDate = select(useGroupingSpot, 
  true->select(spotGroupingReference, 
    nil->nil, 
    default->collapse(groupingSpotDate)), 
  false->nil);
[Default]FTimeBucketAndObject:groupingSpotDiscountDate = rowInstrumentAndTrades:groupingSpotDiscountDate;