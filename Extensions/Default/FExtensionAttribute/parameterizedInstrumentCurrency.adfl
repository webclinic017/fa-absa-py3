[Default]FOption:parameterizedInstrumentCurrency = select(parameterizedCurrency, 
  nil->select(strikeCurrencyAsInstrumentCurrency, 
    true->strikeCurrency, 
    false->currency), 
  default->parameterizedCurrency);