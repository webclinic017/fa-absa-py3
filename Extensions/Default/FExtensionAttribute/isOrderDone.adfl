[Default]FOrderHandler:isOrderDone = select(isExchangeOrder or not exchangeOrdersOnly, 
  true->object.IsOrderDone, 
  false->false);
[Default]FOwnOrder:isOrderDone = switch(orderStatus, 
  "Done"->true, 
  default->false);