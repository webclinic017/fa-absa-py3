[Default]FLeg:inflationReferenceQuotation = select(priceInterpretationType, 
  "All In"->getObject("FQuotation", "Pct of Nominal"), 
  default->select(inflationScalingReference, 
    nil->nil, 
    default->inflationScalingReference:quotation));