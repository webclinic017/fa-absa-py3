[Default]FCashFlowInstrument:useFixing = switch(isFinite(marketPrice), 
  true->true, 
  false->false);
[Default]FRateIndex:useFixing = switch(isFinite(floatRateReferencePrice), 
  true->switch(ignoreFixingInRateIndex, 
    false->true, 
    true->false), 
  false->false);