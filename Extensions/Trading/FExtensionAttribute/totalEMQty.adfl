[Trading]FGroupingOrder:totalEMQty = collapse(totalEMQtyMemberOrders);
[Trading]FInternalMarketOrder:totalEMQty = collapse(totalEMQtyMemberOrders);
[Trading]FOrderProgram:totalEMQty = collapse(totalEMQtyMemberOrders);
[Trading]FOwnOrder:totalEMQty = switch(isOrderActive, 
  true->object.TotalQuantity, 
  default->0);