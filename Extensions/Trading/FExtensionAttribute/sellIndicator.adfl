[Trading]FOrderBook:sellIndicator = shunt(isCalculatedTheoretically, 
  true->switch(double(marketPriceBid) > double(theoreticalPriceAsk), 
    true->1, 
    default->0), 
  false->0);
[Trading]FPriceFeed:sellIndicator = shunt(isCalculatedTheoretically, 
  true->switch(double(marketPriceBid) > double(theoreticalPriceAsk), 
    true->1, 
    default->0), 
  false->0);