[Trading]FOrderBook:buyIndicator = shunt(isCalculatedTheoretically, 
  true->switch(double(theoreticalPriceBid) > double(marketPriceAsk), 
    true->1, 
    default->0), 
  false->0);
[Trading]FPriceFeed:buyIndicator = shunt(isCalculatedTheoretically, 
  true->switch(double(theoreticalPriceBid) > double(marketPriceAsk), 
    true->1, 
    default->0), 
  false->0);