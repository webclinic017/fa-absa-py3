[Trading]FInternalMarketOrder:isOrderActive = switch(orderStatus, 
  "Moved"->true, 
  "PartiallyMoved"->true, 
  default->false);
[Trading]FOwnOrder:isOrderActive = switch(orderStatus, 
  "Active"->true, 
  default->false);