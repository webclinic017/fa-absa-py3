[Trading]FOrderBook:marketEquilibriumOrPhase = switch(isFinite(marketPriceEquilibrium), 
  false->trading.Status.PhaseAbbreviation, 
  default->marketPriceEquilibrium);
[Trading]FTradingInterface:marketEquilibriumOrPhase = switch(isFinite(priceFeed:marketPriceEquilibrium), 
  false->object.Status.PhaseAbbreviation, 
  default->priceFeed:marketPriceEquilibrium);