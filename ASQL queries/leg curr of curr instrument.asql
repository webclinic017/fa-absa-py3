select

i.insid'currency',
c.insid'leg currency',
i.insid = c.insid ? '' : 'Does not match' 'Match'


from
instrument i,
leg l,
instrument c


where

i.instype = 'Curr'

and i.insaddr = l.insaddr
and l.curr = c.insaddr

order by 3 desc