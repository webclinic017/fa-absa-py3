/* Usage code for this ASQL */
select
ael_i(p,'ASQL_log','SAMR_EQStockDivs_yield') 'Name'
into  ASQL_LOG_TEMP
from TextObject p where 
p.name = 'SAMR_EQStockDivs_yield' and p.type = 'SQL Query' 
select
	i.insaddr,
	ael_s(i,'SAMR_EQStockDivs_yield.StockDiv',@Date{},@Outfile{})  
	
from
	instrument i
where
	i.insid = 'ZAR'