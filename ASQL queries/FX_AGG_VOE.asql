select
    t.trdnbr
from
    Trade t,
    Portfolio p,
    Instrument i
where
    t.prfnbr = p.prfnbr
    and p.prfid = 'VOE'
    and t.insaddr = i.insaddr
    and i.instype = 'Curr'
    and t.status not in ('Simulated', 'Void', 'Reserved')
    and t.insaddr ~= t.curr
