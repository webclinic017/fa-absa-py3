SELECT
    ael_i(p,'ASQL_log','MarkitWireLinkNewMatchTrade') 'Name' 
INTO
    ASQL_LOG_TEMP 
FROM     
    TextObject p 
WHERE    
    p.name = 'MarkitWireLinkNewMatchTrade' 
    AND p.type = 'SQL Query' 

SELECT 
    ael_s(, 'MarkitWireLinkNewMatchTrade.LinkNewMatchTrade',@1_FrontReference,@2_MWReference,'false')  'Status'
FROM
	serverdata s
WHERE
	s.srdnbr > 0