/* ******************  ASQL USAGE LOG  ********************** */ 
 select 
      ael_i(p,'ASQL_log','CandiceCashflow') 'Name' 
 into  ASQL_LOG_TEMP 
 from TextObject p 
 where p.name = 'CandiceCashflow' and p.type = 'SQL Query' 
 
  select
creat_usrnbr,
pay_day,
type,
legnbr
from Cashflow