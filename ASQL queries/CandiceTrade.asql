/* ******************  ASQL USAGE LOG  ********************** */ 
 select 
      ael_i(p,'ASQL_log','CandiceTrade') 'Name' 
 into  ASQL_LOG_TEMP 
 from TextObject p 
 where p.name = 'CandiceTrade' and p.type = 'SQL Query' 
 
  select
trdnbr,
quantity,
display_id (t,'counterparty_ptynbr') 'CParty',
counterparty_ptynbr,
insaddr,
creat_usrnbr
from Trade t
