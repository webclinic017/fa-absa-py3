/* ******************  ASQL USAGE LOG  ********************** */ 
 select 
      ael_i(p,'ASQL_log','CandiceYC') 'Name' 
 into  ASQL_LOG_TEMP 
 from TextObject p 
 where p.name = 'CandiceYC' and p.type = 'SQL Query' 
 
  select
   yc.yield_curve_name,
   yc_rate(yc,'17/10/2007','17/10/2007','Simple',,)

   
from
   yieldcurve yc
where 
yield_curve_name='ZAR-SWAP'