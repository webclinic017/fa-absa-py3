/* Usage code for this ASQL */
select
ael_i(p,'ASQL_log','SAMM_CALL_STATEMENTS') 'Name'
into  ASQL_LOG_TEMP
from TextObject p where 
p.name = 'SAMM_CALL_STATEMENTS' and p.type = 'SQL Query'


select
	ael_s(,'SAMM_Statements.GEN_STATEMENTS')			'ok'
from
	serverdata s
where
	s.srdnbr > 0