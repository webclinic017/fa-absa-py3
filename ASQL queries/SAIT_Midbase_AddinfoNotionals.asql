/* Usage code for this ASQL */
select
ael_i(p,'ASQL_log','SAIT_Midbase_AddinfoNotionals') 'Name'
into  ASQL_LOG_TEMP
from TextObject p where 
p.name = 'SAIT_Midbase_AddinfoNotionals' and p.type = 'SQL Query' 


SELECT 
    a.recaddr,
    a.value 
FROM 
    additionalinfo a
WHERE
    addinf_specnbr = 528