/* ******************  ASQL USAGE LOG  ********************** */ 
 select 
      ael_i(p,'ASQL_log','CandiceLeg') 'Name' 
 into  ASQL_LOG_TEMP 
 from TextObject p 
 where p.name = 'CandiceLeg' and p.type = 'SQL Query' 
 
  select
insaddr,
legnbr,
curr,
creat_usrnbr,
nominal_scaling,
type
from Leg
