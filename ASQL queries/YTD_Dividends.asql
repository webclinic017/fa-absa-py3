/*Usage for this code*/
select
    ael_i(p,'ASQL_log','YTD_Dividends') 'Name'
    into ASQL_LOG_TEMP
    from TextObject p where
    p.name = 'YTD_Dividends' and p.type = 'SQL Query'

select
	ael_s(,'YTD_Dividends.ASQL')	  /* Opens AEL */
into
	temp
from
	serverdata s
where
	s.srdnbr > 0 