/* ******************  ASQL USAGE LOG  ********************** */ 
 select 
      ael_i(p,'ASQL_log','CandiceInstrument') 'Name' 
 into  ASQL_LOG_TEMP 
 from TextObject p 
 where p.name = 'CandiceInstrument' and p.type = 'SQL Query' 
 
  select
insid,
instype,
insaddr,
contr_size,
creat_usrnbr
from Instrument