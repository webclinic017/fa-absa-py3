/* ******************  ASQL USAGE LOG  ********************** */ 
 select 
      ael_i(p,'ASQL_log','CAP_MARKET_ABN28_PV') 'Name' 
 into  ASQL_LOG_TEMP 
 from TextObject p 
 where p.name = 'CAP_MARKET_ABN28_PV' and p.type = 'SQL Query' 
 
  select
today 'Day',
i.insid,
present_value(i)/10000 'Present Value'


from 
instrument i

where
i.insid = 'ZAR/ABN28'
