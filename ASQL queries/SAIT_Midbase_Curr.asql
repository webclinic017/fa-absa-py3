/* Usage code for this ASQL */
select
ael_i(p,'ASQL_log','SAIT_Midbase_Curr') 'Name'
into  ASQL_LOG_TEMP
from TextObject p where 
p.name = 'SAIT_Midbase_Curr' and p.type = 'SQL Query' 


SELECT 
    i.insid,
    i.insaddr 
FROM 
    instrument i
WHERE 
    instype = 21