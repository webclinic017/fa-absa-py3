/* update_method=0 */
select distinct
    t.trdnbr
from
    trade t
where
    t.status not in ('Void', 'Simulated')
and match_portfolio(t, 'FIXED INCOME TRADING')
and ael_i(t, 'midasSettlement.is_midas_settled') = 1