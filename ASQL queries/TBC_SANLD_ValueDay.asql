/* Usage code for this ASQL */
select
ael_i(p,'ASQL_log','TBC_SANLD_ValueDay') 'Name'
into  ASQL_LOG_TEMP
from TextObject p where 
p.name = 'TBC_SANLD_ValueDay' and p.type = 'SQL Query' 
select
	t.trdnbr,
	t.value_day
from
	trade t
where
	match_filter(t,@Filter{;Tradefilter.fltid})