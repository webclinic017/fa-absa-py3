select 
    'Price',p.prinbr,i.insid

from 
     instrument i,
	 price p,
	 party par

where
    exp_day>=today
and p.insaddr=i.insaddr
and p.day = '2009-07-29'
and par.ptyid = 'internal'
and p.updat_time > '2009-07-30'